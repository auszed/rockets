"use strict";
var Create = /** @class */ (function () {
    function Create(crearer, acelerarer, frenarer) {
        this.crear = crearer;
        this.acelerar = acelerarer;
        this.frenar = frenarer;
    }
    return Create;
}());
